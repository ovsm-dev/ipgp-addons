IF(MACOSX)
    MESSAGE(STATUS "-- Including OSX Cocoa library with OpenGL")
    INCLUDE_DIRECTORIES(/System/Library/Frameworks)
    FIND_LIBRARY(COCOA_LIBRARY Cocoa)
    MARK_AS_ADVANCED(COCOA_LIBRARY OpenGL_LIBRARY)
    SET(EXTRA_LIBS ${COCOA_LIBRARY} ${OpenGL_LIBRARY})
ENDIF(MACOSX)

IF(SC_IPGPADDONS_GUI_APPS)
	SET(GUI_SOURCES "")
	SET(GUI_HEADERS "")

	SC_ADD_GUI_SUBDIR_SOURCES(GUI 3rd-party)
	SC_ADD_GUI_SUBDIR_SOURCES(GUI client)
	SC_ADD_GUI_SUBDIR_SOURCES(GUI core)
	SC_ADD_GUI_SUBDIR_SOURCES(GUI datamodel)
	SC_ADD_GUI_SUBDIR_SOURCES(GUI map)
	SC_ADD_GUI_SUBDIR_SOURCES(GUI math)
	SC_ADD_GUI_SUBDIR_SOURCES(GUI misc)
	SC_ADD_GUI_SUBDIR_SOURCES(GUI opengl)

	SET(PROJECT_API_H api.h)

	SC_ADD_GUI_LIBRARY(GUI ipgp_qt4)

	SET(GUI_MOC_HEADERS "")
	SET(GUI_UI_HEADERS "")

	SC_LIB_INSTALL_HEADERS(GUI)

	IF(MACOSX)
		SC_LIB_LINK_LIBRARIES(ipgp_qt4 ${QT_LIBRARIES} ${EXTRA_LIBS})
	ELSE(MACOSX)
		SC_LIB_LINK_LIBRARIES(ipgp_qt4 ${QT_LIBRARIES} ${OPENGL_LIRBARIES})
	ENDIF(MACOSX)

	SC_LIB_LINK_LIBRARIES_INTERNAL(ipgp_qt4 client ipgp_core qt4)
ENDIF(SC_IPGPADDONS_GUI_APPS)


